# 2026-07-14

## 翻转棋盘个人博客网站（blog-site）
- 完成互动翻牌棋盘博客：文章列表/详情、标签分类、关于页、Markdown 渲染（自写轻量渲染器）。
- 修复点击交互两处 Bug：
  1. 双击无效：原 `flipToB` 用 450ms 定时器锁 `animating` 吞掉双击第二次点击。改为 transition 方案 + 120ms 去抖，双击可正常切换（第二次点击反转目标状态，动画可中断平滑收起）。
  2. 返回 A 面闪烁：原用 keyframe 动画 + 切换 placeholder 类 + 固定定时器，时序错位。改为 CSS transition（弹簧 cubic-bezier 0.34,1.56,0.64,1），无闪烁。
- Esc 一键翻回：加 `.no-anim` 临时禁用过渡实现瞬间还原。
- 图片替换：A 面用 `封面/0000.jpg` 切 6×5=30 份（cover-crop 到 1200×1000 再切 200×200）；B 面用 `封面/1~30.*`（混合 jpg/png）转 jpg 存入 imagesB。各 30 张就位。
- 图片处理用 Pillow（隔离 venv: ~/.workbuddy/binaries/python/envs/default），已安装。
- 注意：git bash 的 /tmp 实际映射到 C:\Users\38488\AppData\Local\Temp，Python 用该真实路径。
- AUDIO 当前用占位静音 base64，可在 flipboard.js 顶部 AUDIO_SRC 替换为真实音频路径。

## 00:45 — 棋盘布局改为 5×6 + 精确切割
- **棋盘维度**：从 6列×5行 改为 5列×6行（匹配源图0000.jpg的实际排布）
- **修改文件**：`flipboard.js`（COLS=5, ROWS=6）、`styles.css`（grid-template-columns: repeat(5, 1fr)）
- **A面切割**：重写 `smart_cut_5x6.py`，用空白缝隙检测策略：
  - 先定位标题"30天推歌挑战"底部
  - 均匀网格初始化 → 内部切割线微调到最近空白处
  - 结果：30/30 格子完整，切割线密度全 < 11，文字无截断
- **主题名称**：已统一为「30天推歌挑战总结」

## 00:49 — A面改为白底黑字文字 / 格子统一正方形
- **需求变更**：每格统一正方形大小；A面不再用切割图片，改为白底黑字文本；B面图片不变
- **CSS**：`.cell` 加 `aspect-ratio: 1/1`；`.face-a` 改白底(#fff)黑字，含 `.cell-day`(标题) + `.cell-desc`(描述)；删除 `.board` 的 aspect-ratio
- **OCR**：装 `rapidocr-onnxruntime`，写 `ocr_cells.py` 复用5×6网格线逐格OCR提取文字，人工校正识别误差（数宇→数字、一重唱→合唱等）
- **数据**：`js/cell-texts.js` 存30条 {day, desc}
- **flipboard.js**：face-a 改为渲染文字div（不再用img）；index.html 引入 cell-texts.js

## 02:04 — 压缩B面/删A面/简化页面
- **B面压缩**：PIL 最长边300px + quality78，1229KB→466KB（省62%），原地覆盖 imagesB/01~30.jpg
- **删A面**：rm -rf imagesA（A面已是文字，无引用）
- **简化**：
  - 删除标签页：nav链接、#tags section、renderTags/filterByTag 函数、文章卡/详情的tag渲染、popstate tag分支、hash #tag- 分支
  - 关于页社交链接：socialLinks 改为单条 网易云 🎵 → https://y.music.163.com/m/user?id=12750379396
  - flipboard.js 移除 IMAGE_PATHS.a（imagesA）残留
- 校验：5个JS语法OK，grep无残留引用，项目总604K

## 02:18 — 打包手机版
- **单文件 HTML**：写 /tmp/build_single.py，把 css/js 内联、30张B面图转 Base64，生成 `30天推歌挑战-手机版.html`（673KB，data:image/jpeg×30，无残留 src/href 引用）
- **zip**：`blog-site.zip`（481KB，37文件）供部署/备份
- 目的：手机传输后浏览器直接打开单文件 HTML，免解压、免相对路径

## 02:27 — 修复单文件HTML图片不显示
- **现象**：单文件HTML打开无图片，zip解压正常
- **根因**：build_single.py 的 new_block 末尾写成 `"\n  ];`（少闭合 `}`），内联后 flipboard.js 语法错误(`Unexpected token ';'`)，整段不执行→图片数组未加载。zip版用原文件故正常
- **修复**：new_block 改为 `"\n  ] };`，重新生成。校验：6个内联script块语法全OK，30个data:image/jpeg均在
- **教训**：内联JS到<script>时务必用 node 校验每块的 `new Function` 语法

## 18:38 — 替换day6的B面图
- **来源**：`Desktop/瑶湘呼应/OIP-C.webp`（474×436，手写风格封面图）
- **操作**：转jpg（最长边300px + 质量78）→ 替换 `imagesB/06.jpg`（300×275, 16KB）
- **同步更新**：重新生成 zip(497KB)、单文件HTML(686KB)，校验全部通过

## 21:42 — 生成歌曲资料汇总Word文件
- 搜齐 Day 25-30（方大同《爱在》、徐佳莹《雏形》、AGA《孤雏》、鬼卞《乱世》、薛之谦《一半》、张国荣《我》国语版）
- Day 14 两个网易云婚礼歌单（B面 id=18094010596 / A面 id=18093968579，creatorId=12750379396）
- 用 python-docx 生成 `30天推歌挑战-歌曲资料汇总.docx`：30个Day标题 + 145段落（每首含 出处/创作/背景/推歌语，部分标版权/年份待核），44K
- 目的：上下文将满，存盘防遗忘；后续可接入网站B面/文章详情

## 22:56 — 新增歌曲弹窗 + 听歌按钮
- **song-data.js**：新增30天歌曲数据(singer/song/intro/platform/link)；DAY2/DAY14 platform='bilibili'、link留空待用户补B站链接；其余netease，link空时按钮自动跳网易云搜索
- **flipboard.js**：单击A面翻到B时 openModal 弹出歌曲弹窗（左图右文）；点击弹窗外区域 closeModal（克隆图片缩小飞回对应格子B面 + 其余淡出，FLIP动画）；Esc优先关弹窗；新增 updateListenButton/resetListenButton，翻转时按钮文案/字体加粗变化
- **index.html**：flipboard-controls 加 listenBtn(含 listen-icon 槽位🎵占位)；body 末加 songModal 弹窗结构；script 引入 song-data.js
- **styles.css**：modal-overlay/card/left/right/close 样式 + btn-listen(红)/.active/listen-icon 槽位（注释提示替换background-image）；移动端弹窗竖排
- 重建单文件HTML(704KB)与zip(498KB)，7个内联脚本语法全OK

## 23:42 — 换上网易云/B站图标 + 删除按钮小表情
- 图标来源：Desktop/瑶湘呼应/ 的 wyy.png（网易云）、bili.webp（B站）；复制到 blog-site/images/。
- flipboard.js 新增常量 ICON_NETEASE='images/wyy.png'、ICON_BILI='images/bili.webp'；外置 listenBtn 图标按平台用 JS 切 background-image（默认网易云，resetListenButton 还原），弹窗内链接按钮改 <img class="modal-link-icon"> 显示平台图标。
- 删除按钮文字边上的 🎵/🎬 小表情：updateListenButton/resetListenButton 文案去 emoji；renderModalLinks 改为图标 <img> + 纯文字；index.html 的 listenIcon 空 span、listenText 默认「去听听这首」；CSS .listen-icon 改图标框（不再用 emoji 文字），新增 .modal-link-icon。
- build_single.py 增加「全局替换 images/wyy.png / images/bili.webp 为 Data URI」；注意 CSS 注释勿写图标路径字面量（否则被重复内联）。
- 重建单文件HTML(807KB)与zip(572KB)，每图标仅内联1次，7内联脚本语法OK。

## 23:08 — 弹窗隐去方式 + 平台修正 + 听歌按钮修正
- **弹窗隐去方式**：确认逻辑——字体/弹框简单淡出即可；图片用 closeModal 的克隆FLIP动画「缩小并重合到对应格子B面」（已实现，保留）。
- **平台修正**：DAY14（婚礼歌单）改回 netease（非bilibili），并加 `links` 双歌单：婚礼A面 id=18093968579 / 婚礼B面 id=18094010596（creatorId=12750379396，用户原给）；DAY15（烟之暗面）改 bilibili；DAY2 仍为 bilibili（均无链接，待用户补B站URL）。
- **DAY17（陈奕迅DUO演唱会）** 搜得网易云全场歌单 id=40458084（38首正确顺序），标记 playlist:true，link 指向该歌单。
- **听歌按钮修正**：
  1. 关闭弹窗后「保持链接」——closeModal 不再调 resetListenButton，外置 listenBtn 保留最近一次翻开格子的链接（仅「全部翻回」时重置）。
  2. 弹窗右侧下半部新增「去网易云/去B站」链接按钮区（#modalLinks，renderModalLinks 动态渲染）：单链接1个按钮；DAY14双歌单2个按钮（A面/B面各一）；bilibili无链接显示「B站链接稍后补充」；netease无链接跳搜索。
- 重建单文件HTML(724KB)与zip(510KB)，7个内联脚本语法全OK；附 build_single.py 于项目根便于后续重打包。
