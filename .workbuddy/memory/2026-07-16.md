# 2026-07-16 工作日志

## 20:43 — 署名弹窗改为纵向排列布局（三选项→展开操作区）
- 需求：署名弹窗从横排按钮改成**纵向排列**——第一行问题文字，第二行横排三按钮(填写昵称/匿名投放/取消)；点匿名后隐藏初始三按钮、展开"确认投放+取消"；点填写昵称后隐藏初始三按钮、展开输入框+"确认投放+取消"。
- 改动：
  - HTML：`#driftNameModal` 内部重构为 `.drift-name-q`(问题) + `#driftNameOptions`(三按钮:填写昵称/data-mode=name / 匿名投放/data-mode=anon / 取消/id=driftNameInitialCancel) + `#driftNameInputWrap`(输入框,hidden) + `#driftNameActions`(确认+取消,hidden)。删除旧 `.drift-name-row` 和独立 `.drift-name-cancel`。
  - CSS：`.drift-name-card` 改 `display:flex;flex-direction:column;gap:16px`(纵向)；新增 `.drift-name-options{display:flex;gap:10px;justify-content:center}` 横排三按钮；`.drift-name-btn-cancel` 样式(无边框幽灵取消按钮)；`.drift-name-input` 居中输入框；`.drift-name-actions{display:flex}` 横排操作按钮。删除旧 `.drift-name-row/.drift-name-cancel`。
  - JS：`openDriftNameStep()` 重置为显示 options 行 + 隐藏 input/actions 两区；`initDrift` 署名弹窗绑定改用新结构——options 按钮 click 隐藏自身 + 按 data-mode 展开对应区(actionsEl.dataset.pendingMode 记录当前模式)、confirmBtn 按 pendingMode 调 finalizeDrift(true/false)、两个取消按钮各绑 closeDriftNameStep。
- 校验：node --check OK；重建 HTML=1333803B / zip=905246B；grep 确认 driftNameOptions/driftNameActions/driftNameInitialCancel/driftNameActionCancel 共 10 处，旧 driftNameRow/driftNameStep/driftNameCancel" 为 0。

## 20:34 — 署名改为独立弹出窗（时间先后，非空间罗列）
- 需求：署名小板块初始不出现，点「投放」后弹出**独立新窗口**(覆盖在漂漰弹窗之上)；昵称输入框仅在点击「填写昵称」后才出现(时间顺序，非空间下罗列)。
- 改动：HTML 把 `#driftNameStep` 从 `#driftModalForm` 内移出，新增独立 `.modal-overlay#driftNameModal`(含 `.drift-name-card` 小卡片：问题+[匿名]/[填写昵称]+昵称输入[隐藏]+确认投放+取消)。JS — `openDriftNameStep` 改为 `driftNameModal.classList.add('open')`(不再隐藏投放按钮)；`closeDriftNameStep` 改为移除 open；`initDrift` 绑定改指向 `#driftNameModal`(含遮罩点击/ESC 关闭)；无 `driftNameStep` 残留引用。CSS — `.drift-name-step` 替换为 `.drift-name-overlay{z-index:1100}` + `.drift-name-card{width:min(360px,90vw);overflow:visible;padding:22px...}` + 居中标题/取消。node --check OK，已重建。

## 20:30 — 漂漰推荐板块放宽空间
- 署名已是最后一步弹窗(点投放才出现，无需改)。需求：给中间"其他用户推荐"板块更多空间。
- 放大间距：`.drift-modal-list` padding 16/20→22/24、gap 12→18；`.drift-bottle` padding 16→22/20、圆角14→16；`.drift-bottle-song` 1.2→1.35rem+letter-spacing；`.drift-bottle-reason` margin-top 7→12、0.88→0.98rem、line-height 1.45→1.6；`.drift-bottle-by` margin-top 8→14、0.82→0.85rem。node --check OK，已重建。

## 20:25 — 漂流瓶弹窗布局修正（纵栏→横栏三板块）
- 根因：`.modal-card { display: flex }`（为歌曲弹窗左图右文设计）被漂漰弹窗复用，导致子元素**横排**，曲目特点挤到左边变成纵栏("适合/开车/时听/的/歌"竖排)。
- 修复：`.drift-modal-card` 覆盖为 `display:flex; flex-direction:column; max-height:86vh; overflow-y:auto`(纵向三板块)；`.drift-modal-day` 改 `display:none`(内容合并到顶栏)；`.drift-modal-feature` 改紧凑顶栏样式(去 nowrap/ellipsis、`flex-shrink:0`)；`.drift-modal-list` 改 `flex:1; min-height:0`(自适应填充中间区)；JS `openDriftModal` 中 featEl 合并显示 `label + '   ' + feat`(如 "DAY 7   适合开车时听的歌")。node --check OK，已重建。

## 20:12 — 漂流瓶弹窗界面重排
- 需求：曲目特点改成弹窗顶栏满铺低横栏(借 `--feature-bg` 变量，与歌曲弹窗一致，暗色不再偏亮)；推荐卡片只显示 歌名+理由+昵称(去掉曲目特点重复)；表单改为「推荐曲目(单行小框)+推荐理由(多行大框)+投放」，投放后再弹署名步骤(匿名 / 填昵称)。
- 改了三处：index.html 重排 `#driftModal` 结构(顶栏 `.drift-modal-feature` 直接子节点满铺、`.drift-modal-day` 自带内边距、form 内 song input + reason textarea + 投放按钮 + `#driftNameStep` 署名面板)；app.js — DRIFT_SEED 补 `reason`、driftBottleCardHtml 去 day/feature 行加 reason、handleDriftModalSubmit 改两步(校验→openDriftNameStep)、新增 finalizeDrift/closeDriftNameStep、initDrift 绑定署名按钮、openDriftModal 开头 closeDriftNameStep 复位；css 改 `.drift-modal-feature` 满铺低栏、`.drift-input-reason` 多行、`.drift-submit-btn` 全宽、`.drift-name-*` 面板、`.drift-bottle-reason`，并加 `--drift-bottle-reason`(三主题)。node --check OK，已重建。

## 20:06 — 暗色下"曲目特点"横栏(.modal-feature)偏亮
- 根因：`--feature-bg/-border/-text` 三处定义(:root 行73-75、[data-theme=dark] 行147-149、@media dark 行216-218)值全相同(#f3f0ff/#e2dcff/#4f46e5)，暗色块未单独调暗，注释还写"两种主题一致"。
- 修复：只改后两处暗色块为深紫——bg `#2a2545`、border `#3d3568`、text `#c4b5fd`；:root 浅色 `#f3f0ff` 保留。用行号+缩进精确定位(脚本 assert 校验)，避免误改 :root。重建后 HTML 内 `#2a2545`×2 / `#f3f0ff`×1。

## 00:13 — 排查碎碎念 DAY19「边缘状态 / 每一个清晨」分段丢失
- 现象：用户在 JS 源文件(blog-data.js DAY19 生命之诗赏析)看到"边缘状态。"与"每一个清晨"之间有分段，但网站上渲染出来没有分段。
- 实测 markdown.js 段落逻辑：每一行非空文字都被包进独立 `<p>`（第46行 `^(?!<)(.+)$`）；因此**只要两句不在同一物理行(中间有空行)，就必分段**。用实际文本跑了两种情况验证：有空行→两段 `<p>`；同一行→一段。
- 关键根因：**用户在做手动微调时只改了 `blog-data.js` 源码，未重新打包单文件 HTML**，网站打开的是昨晚 23:33 的旧构建（旧构建里该行还是 `*每一个清晨` 单星号，证实源码后被手动改成 `**`）。旧构建其实已含 `\r\n\r\n` 分段，但用户后续手动改动未进 HTML。
- 修复：重新 `build_single.py`（blog-data.js / flipboard.js node --check 均 OK），产物 HTML=1302688B / zip=898220B。验证新 HTML 含 `边缘状态。\r\n\r\n**每一个清晨`，分段 + `**` 加粗均在。
- **用户工作流结论（重要）**：用户习惯直接手改 `blog-site/js/*.js` 源码微调文案，但站点实际使用的是 `30天推歌挑战-手机版.html`（单文件/zip），每次改源码后必须重跑 `build_single.py` 才会生效；浏览器还可能缓存旧 HTML，需硬刷新(Ctrl+Shift+R)。已告知用户此流程。

## 19:04 — 曲目特点信息栏改版（去小条贴图 → 文本区顶部彩色标题）
- 用户反馈：弹窗曲目特点"只有一部分有" + 浅灰底小条区块"像贴图不好看"。
- 澄清：「只有一部分有」是设计如此——目前仅 DAY7 在 song-data.js 填了 `feature` 字段，其余 29 天未填故不显示；非 bug。
- 样式改动（styles.css `.modal-feature`）：去掉 `background:#f4f4f8`+`border-left` 小条 box 与 `::before '曲目特点 · '` 前缀；改为文本区顶部**更大彩色标题**（`font-size:1.05rem;font-weight:700;color:var(--primary,#6366f1)`）+ 一条 `border-bottom:1px solid #ececf3` 细分割线划分文本区，无前缀。
- 顺带统一漂流瓶弹窗：app.js openDriftModal 里 `driftModalFeature` 原 `feat ? ('曲目特点 · '+feat):'这一天'` → 改为有 feature 时仅显示文本（隐藏前缀）、无 feature 时 `hidden` 隐藏。移除"曲目特点 · "前缀形式（用户明确不喜欢该形式）。
- 校验：node --check app.js/flipboard.js 通过；重建 HTML=1315748B / zip=900956B；grep 确认可见处"曲目特点 · "残留=0（仅源码注释 2 处，不可见）。

## 19:15 — 曲目特点改为「右侧上下双色块」硬分割（避雷 PILL）
- 用户明确需求：① 分割线以上是带底色块、以下歌名/简介纯白；② 视觉上右侧分成两个色块（上=曲目特点，下=其他文本）；③ 绝不出现之前 PILL（白底包裹文字的浮起小条）观感。
- 实现（styles.css `.modal-feature`）：用负边距 `margin:-22px -22px 0 -20px` 抵消 `.modal-right` 的 padding（22/22/22/20），让顶部色块满铺整个右侧文本区；底色 `background:#f3f0ff`（淡紫），下边框 `1px solid #e2dcff` 作分割线；卡片本身 `overflow:hidden;border-radius:14px` 自动裁切圆角 → 形成真正的"上色块/下白块"硬分割，非浮起贴图。
- 配套：`.modal-song` 加 `margin-top:16px`，让歌名与分割线之间留白（避免紧贴）。

## 19:39 — 全站主题改为「浅色默认 + 跟随系统双色调 + 手动切换」
- 需求：把网站从暗色调改为亮色调，或做成可跟随系统的双色调。最终两种都做了。
- 实现：styles.css `:root` 改为浅色变量集（--bg-page:#f4f5f9 / --bg-card:#fff / --text-primary:#1e2330 等）；新增 `[data-theme="dark"]` 与 `@media (prefers-color-scheme: dark):root:not([data-theme="light"])` 两套暗色变量。原硬编码深色表面改为变量：navbar(--navbar-bg)、modal-overlay(--overlay-bg)、btn-ghost(--ghost-bg/-hover)、kbd(--kbd-bg)、code/pre/th(--code-bg/--code-block-bg/--code-color)、input(--input-bg/-text)、modal-feature(--feature-bg/-border/-text)。原 `--bg-dark` 全部改名 `--bg-page`。
- 切换：index.html 加① head 内联防闪烁脚本（按 localStorage / 系统偏好提前设 data-theme）；② 导航栏 `theme-toggle` 按钮（🌙/☀️）；③ DOMContentLoaded 内 initThemeToggle：手动点击持久化到 localStorage('site-theme')，未手动选择时跟随系统变化。
- 设计取舍：歌曲弹窗与漂流瓶子弹窗**保持白底卡片**（自带深色文字，双主题均可读），仅页面/导航/卡片/页脚/输入框/遮罩随主题变。
- 校验：theme_fix.py 原子替换（含唯一性断言）通过；node --check 未动 JS；重建 HTML=1323694B / zip=903168B；grep 确认 built HTML 含 toggle 按钮、site-theme 逻辑、浅色默认变量、暗色媒体查询。
- 边界：无 `feature` 的日期 `featEl.hidden=true`（display:none），不会留下空色块。
- 校验：重建 HTML=1316046B / zip=901111B；grep 确认 满铺负边距/底色/分割线/歌名上间距 均在。

## 00:55 — 碎碎念返回目录定位到刚看文章（中上方）
- 需求：从碎碎念具体文章点「返回」回到目录时，不要每次滚回最顶，而是定位到刚看的那篇并置于页面中上方。
- 根因：`app.js` 的 `showPage()` 第21行 `window.scrollTo(0,0)` 强制回顶；文章卡片无锚点可定位。
- 改动（全在 `blog-site/js/app.js`，共6处）：
  1. 新增模块变量 `lastViewedArticleId` 记录最近查看的文章 id。
  2. `navigateToArticle()` 进入文章时写入 `lastViewedArticleId = id`。
  3. `renderArticles()` 卡片模板加 `data-article-id="${article.id}"` 锚点。
  4. `showPage(id, opts)` 支持 `opts.anchorId`：切到 articles 且带 anchorId 时调用 `scrollToArticle()`，否则仍滚顶（不影响首页/留言页）。
  5. 新增 `scrollToArticle(id)`：requestAnimationFrame 后取卡片位置，`window.scrollTo({top: rect.top + scrollY - innerHeight*0.25, behavior:'smooth'})`，使其处视口中上方；不足则 Math.max(0,...) 兜底到顶。
  6. 「返回」按钮 `articleBackBtn` 与 `popstate`（浏览器后退）均传入 `anchorId: lastViewedArticleId`，返回目录即定位。
- 验证：node --check OK；单文件 HTML grep 四项全 true（scrollToArticle 定义 / anchorId 定位 / data-article-id / innerHeight*0.25）；HTML=1304333B / zip=898876B。
- 说明：顶部导航主动点「碎碎念」仍走滚顶（不带 anchor），符合"从头浏览"预期；仅"从文章返回"才定位。

## 11:00 — 删除三篇碎碎念文章(id 1/3/4)
- 用户要求删除三篇碎碎念：「写在前面：这个 30 天推歌挑战是什么」(id:1)、「翻牌交互是怎么做的」(id:3)、「关于聆听：怎么听这 30 首」(id:4)。
- 用 Python 脚本按 id 正则精确删除 articles 数组中这三个对象(保留数组逗号/结构正确)：articles 从 19 篇降到 16 篇；node --check 通过。
- 安全性核查：① 顶部 `tags` 数组的 intro/dev/music 是分类标签(非文章)，删文后变空标签但未一并删除，保留无害，已提示用户可选清理；② 核查 song-data.js 全部 `note` 跳转指向 13/14/15/16/18/19，**无指向 1/3/4**，故删除后不会让任何歌曲简介的「查看碎碎念」按钮失效。
- 重建：HTML=1300025B / zip=897285B；单文件 HTML 验证三标题均"已删除"。

## 12:30 — 新增「漂流瓶」功能 + 弹窗曲目特点信息栏
- 用户需求：① 漂流瓶——游客在某天推荐规则下留下想推荐的歌名，随机查看他人推荐，可署名/匿名；② 歌曲简介弹窗顶部加「曲目特点」信息栏（如 DAY7「适合开车时听的歌」）。
- 决策（AskUserQuestion）：存储用**本地演示版**(localStorage + 预置示例瓶 DRIFT_SEED)，非真后端；入口为顶部导航新增「🍶 漂流瓶」独立标签。
- 漂流瓶改动：
  - index.html：navMenu 加 `💬 留言` 后加 `🍶 漂流瓶`(data-page=drift)；新增 `section#drift`：捞瓶展示区(driftCatchCard + 🎲随机捞 + 按天筛选 select) / 投瓶表单(driftDay 选天 + driftSong 歌名 + driftName 昵称 + driftAnon 匿名) / 我的瓶子列表(driftMineList)。
  - app.js：新增「漂流瓶」模块——DRIFT_KEY(localStorage)、DRIFT_SEED(13条预置示例瓶，扮演"其他游客")、driftPublicPool=SEED+本机瓶、driftDraw(随机捞/按天过滤)、renderDriftMine(我的瓶子+删除)、handleDriftSubmit(投放)、fillDriftDaySelects(JS生成 DAY1-30 选项，带 feature 副标)、initDrift(绑定+匿名禁用昵称)；并在 `init()` 中加 `initDrift()`。风格复用留言区的 escapeHtml/fmtTime/localStorage 模式；day=0 表示"不指定/不定期"，day>=1 联动显示 SONG_DATA[day-1].feature 作副标。
- 信息栏改动：
  - song-data.js：顶部字段注释加 `feature` 说明；DAY7(Rammstein 对象)加 `feature: '适合开车时听的歌'`(用户示例)。
  - index.html：modal-right 顶部加 `<div class="modal-feature" id="modalFeature" hidden>`。
  - flipboard.js `openModal`：渲染 feature——有则显示主题色 pill(🎯前缀)，无则 hidden。
  - styles.css：`.modal-feature`(紫色渐变 pill) + 漂流瓶全套样式(.drift-*)；置于响应式分区前。
- 验证：node --check app/flipboard/song-data 三文件均 OK；build_single.py 成功(HTML=1314634B / zip=900768B)；单文件 HTML grep 六项全 true(🍶漂流瓶 / id="modalFeature" / 适合开车时听的歌 / DRIFT_SEED / initDrift / .modal-feature)。
- 局限提示（交付时告知用户）：本地演示版下"其他游客"实际=预置示例池 DRIFT_SEED + 本机留的瓶子，非真实跨用户；真共享需上线接后端(注释已预留接口位置)。其余 29 天 feature 待用户按 `feature:'...'` 格式补，未填不显示信息栏。

## 13:05 — 漂流瓶改「玩法模式」+ 信息栏去渐变/emoji（用户二次调整）
- 用户反馈：① 紫色渐变 pill + 🎯 不好看，要简洁；② 漂流瓶不要作独立板块，要作为「玩法」与「猜一猜」并列；③ 交互改为：点漂流瓶按钮→进入玩法→点某天窗格→弹出该天游客推荐曲目，并可自己发一条（解决推荐曲目与推荐原因对应关系）。
- 信息栏改动：`.modal-feature` 由紫色渐变 pill(`border-radius:999px`+🎯 `::before`)改为文本区顶部**背景色区分的简洁区块**(`background:#f4f4f8`+左侧 3px 主色竖条+`::before:'曲目特点 · '` 灰字标签，无 emoji)。JS 仍用 `featEl.textContent=d.feature` 不变。
- 漂流瓶重构（从独立页面 → 玩法）：
  - index.html：删顶部 `🍶 漂流瓶` 导航标签与整个 `section#drift`；在 `flipboard-controls` 的 `#guessBtn` 旁加 `#driftBtn`(btn-drift, 🍶 漂流瓶)；`#guessBanner` 后加 `#driftBanner` 横幅；新增 `#driftModal` 弹窗(头部 driftModalDay/Feature + driftModalList 列表 + driftModalForm 表单[driftModalDayVal 隐藏域 + driftModalSong + driftModalName + driftModalAnon + 投放按钮] + driftModalTip)。
  - flipboard.js：加 `let driftMode=false`；`handleCellClick` 在 guessMode 分支后加 driftMode 分支(调 `BlogApp.openDriftModal(index)`，不翻牌不播歌)；新增 `toggleDrift/handleDriftClick/exitDriftMode/showDriftBanner/hideDriftBanner`，与 guessMode **互斥**(互切时先退出对方)；`init()` 接线 `#driftBtn`；公共 API 暴露 `toggleDrift`。
  - app.js：删除旧页面函数 `driftDraw/renderDriftMine/fillDriftDaySelects/handleDriftSubmit/showDriftTip/旧 initDrift`；新增 `openDriftModal(dayIndex)`(按天打开弹窗、填头部、渲染该天列表)、`renderDriftModalList(day)`(过滤 day 一致+绑定删除)、`isMineBottle`、`handleDriftModalSubmit`(投放到该天)、`closeDriftModal`、`showDriftModalTip`(改为 #driftModalTip)、`initDrift`(绑定表单/关闭/匿名禁用/Esc)；DRIFT_SEED 去掉 day:0 那条(不指定项在按天弹窗无意义)，保留 12 条；BlogApp 暴露 `openDriftModal`。
  - styles.css：删除旧 `.drift-container/.drift-catch*/.drift-form*/.drift-list/.drift-mine-empty/.drift-select` 等页面样式，新增 `.drift-banner`/`.btn-drift`(含 active 变橙)/`.drift-modal-*`(头部/列表/表单/空态)/复用 `.drift-bottle*` 卡片；`.drift-modal-card` 宽 `min(460px,94vw)`。
- 验证：node --check app/flipboard/song-data OK；build_single.py 成功(HTML=1315812B / zip=900970B)；单文件 grep 11 项全 ✅(driftBtn/banner/modal/openDriftModal/driftMode/handleDriftClick/旧drift页面已删/旧drift导航已删/无🎯/背景块#f4f4f8/曲目特点标签)。
- 注意：旧 `#drift` 页面所有 id(driftForm/driftDayFilter/driftMineList 等)已从源码移除，无悬空引用。

## 19:21 — 给每一天补 `feature`（曲目特点）字段，内容取自推介词.docx 的「」标题
- 来源：用户微信发来的 `推介词.docx`（D:\微信\...\2026-07\推介词.docx）。该 docx 每篇形如「DAY 1 「在歌名里带有颜色的歌」」——「」内即当天的"A面文本"/推荐主题，正好作为弹窗 `feature` 一行字。
- 提取方式：docx 是 zip，用 python zipfile 读 word/document.xml，按 `<w:t>` 拼段落；"DAY" 是**粗斜体数学字母**(U+1D468-1D481)、数字是**粗体数学数字**(U+1D7CE-)，需归一化才能正则匹配。归一到 ASCII 后逐日抽出「」标题。
- 落地：写 Python 脚本，按 `^  \{`（行首2空格+`{`）定位 song-data.js 的 30 个顶层条目，依数组顺序(i+1=DAY号)在 `{` 后插入 `feature: 'THEME', `。THEME 中数学粗体数字（如 DAY12/13 的 𝟏𝟑、𝟕𝟎）归一化为 ASCII「13」「70」以保证网站正常渲染（docx 里是样式字符，非内容）。
- 结果：25 天成功写入（DAY7 原本就有 feature 被脚本跳过保留）。node --check OK；build_single.py 重建 HTML=1317130B / zip=901487B；单文件 HTML grep `feature:`=25 且样本（让你心碎的歌 / 来自你青春期前（13岁）的歌 / 你喜欢的来自70年代的歌）均在。
- **缺失 5 天无 A面文本**：docx 本身只有 25 天主题，缺 DAY17(陈奕迅《DUO 演唱会全曲》)、DAY23(唐朝乐队《国际歌》)、DAY28(鬼卞《乱世》)、DAY29(薛之谦《一半》)、DAY30(张国荣《我（国语版）》)。已交付 25 天并询问用户提供这 5 天主题。
- 注意：这次脚本插入把 feature 放在条目首行（`  { feature: '...',  singer:...`），与 DAY7 原 feature 在续行不同，但均合法；渲染不受影响。

## 19:35 — 更正：A面主题权威来源是 cell-texts.js（非 docx）
- 用户指正：A面=棋盘A面，主题文字=棋盘每格 DAYxx 下面那行小字，必定存在于网站文件里，不该只在 docx 搜。
- 找到 `blog-site/js/cell-texts.js`：`const CELL_TEXTS = [{day, desc}, ...]` 共 30 条，`desc` 正是棋盘 A 面白底黑字那行（文件头注释明写"A面白底黑字用"）。这才是权威 A面文本，含之前 docx 缺的 DAY17/23/28/29/30。
- 重做：写 `fix_feature.py`（因 heredoc 会破坏正则反斜杠，改写文件再跑），解析 CELL_TEXTS，把 song-data.js **全部 30 天** feature 统一设为对应 `desc`，并移除旧 docx 版差异（如 DAY3 由"让你想起夏天的歌"→"会让你想起夏天的歌"，DAY20"对你"→"对于你"，DAY26"想陷入"→"想要陷入"），使弹窗曲目特点与棋盘 A 面显示完全一致。
- 校验：node --check OK；build_single.py 重建 HTML=1317377B / zip=901582B；单文件 HTML grep `feature:`=30，且 DAY17/23/28/29/30 与 DAY3/20/26 均已对齐 cell-texts.js。已清理临时脚本。
- 结论：全 30 天曲目特点现已就绪，不再缺漏。

## 19:52 — 暗色模式下棋盘 + 简介弹窗仍为白底 → 全面主题化
- 用户反馈：之前"弹窗保持白底卡片"的取舍不彻底——暗色模式下**棋盘格子、A面、点开格子后的歌曲简介弹窗**仍是白底且不随模式切换，与整体暗色割裂。
- 根因：这些元素在 styles.css 里是**硬编码白色**（`#ffffff`/`#fff`/`#1a1a1a`/`#111`/`#333`/`#555`/`#999`/`#e2e2ea`/`#6d28d9`/`#334155`），没接主题变量。
- 新增主题变量（`:root` 浅色 / `[data-theme="dark"]` / `@media(prefers-color-scheme:dark)` 三处各一份）：`--cell-bg`(格子+A面底) / `--cell-text` `--cell-text-2`(A面文字) / `--modal-bg`(弹窗卡底) / `--modal-text` `--modal-song` `--modal-singer` `--modal-intro` `--modal-hr` `--modal-note`(简介文字) / `--drift-bottle-bg` `--drift-btn-bg`/`text` `--drift-day` `--drift-feature` `--drift-empty` `--drift-input-bg`/`text` `--drift-bottle-day`/`song`/`by`(漂流瓶弹窗内部)。
- 替换：`.cell`/`.face-a`/`.cell-day`/`.cell-desc`/`.face-a.img-failed` → cell 变量；`.modal-card`/`.modal-right`/`.modal-song`/`.modal-singer`/`.modal-intro`/`.modal-intro-hr`/`.modal-link-note` → modal 变量；`.drift-modal-*`（head/form 边框、day/feature/empty、input、btn、bottle 卡片/文字、del 按钮）→ drift 变量。`.drift-bottle-mine` 暖色#fff7ed 保留作高亮。`.modal-feature` 仍刻意保持淡紫#f3f0ff 双色块（用户设计的硬分割）。
- 详情：暗色下 `--cell-bg:#243043`（比棋盘包装 #1e293b 略亮以区分格子）、`--modal-bg:#1e293b`、drift 卡深紫渐变 #232a45→#1f2742。媒体块内 DARKVARS 因 4 空格嵌套被按 2 空格插入（缩进不一致但功能无碍）。
- 校验：三个 theme_fix 脚本均带唯一性断言通过；`.howto-card` 本就用 `var(--bg-card)` 早已跟随主题；剩余 `#fff` 仅图标底/音乐 iframe/紫色按钮渐变（双主题可读，无需改）。build_single.py 重建 HTML=1326245B / zip=903516B；grep 确认 `var(--cell-bg)`/`var(--modal-bg)`/`var(--drift-bottle-bg)` 均进 built HTML。
- 结论：棋盘、A面、歌曲简介弹窗、漂流瓶弹窗现已全部跟随深浅模式；右上角 🌙/☀️ 切换或跟随系统均一致。

## 21:03 — 碎碎念删除 6 个待补充板块 + 末尾两板块对调
- 需求：删掉碎碎念里待补充(placeholder)的 6 个板块，然后把删完之后的最后两个板块顺序换一下。
- 6 个待删：`blog-data.js` articles 数组中 `/* 预留给你的空白模块 */` 注释组——id 5(我的推歌理由合集)/6(某一首歌背后的故事)/7(最难忘的瞬间)/8(私藏歌单)/9(婚礼歌单由来)/10(新计划)，均 `placeholder:true`、标题带"（待补充）"。
- 注意：id 18「写在最后：30天推歌挑战的结尾总结」虽也 `placeholder:true`，但属另一组(推荐词文档移入组)，不在用户数的 6 个内，保留。
- 操作：① 删除 id 5-10 整段(含注释)；② 删除后数组末尾剩 id 18(写在最后) 与 id 19(DAY 29)，对调使 id 19 在前、id 18 在后。
- 校验：node --check OK；重建 HTML=1331309B/zip=904889B；grep 确认 6 个待补充标题 0 残留、预留空白模块注释 0 残留、源文件 id:19(行208) 在 id:18(行222) 之前。articles 总数由 16→10。
- 安全性：song-data.js 全部 note 跳转仅指向 13/14/15/16/18/19，无指向 5-10，删除不影响歌曲简介「查看碎碎念」按钮。

## 21:19 — 删除 DAY 4 推歌小吐槽碎碎念
- 需求：删除第四天的推歌小吐槽。
- 操作：① `blog-data.js` articles 删除 id 13「DAY 4 · 推歌小吐槽」整段(emoji 😅, excerpt/练琴回寝)；② 同步在 `song-data.js` 第 35 行陈奕迅《时光隧道》(DAY 4, feature「让你想起你宁愿忘记的人的歌」) 条目的 `intro` 后移除 `, note: 13`，避免简介「查看碎碎念」按钮指向已删死链。
- 校验：node --check OK（两文件）；重建 HTML=1330913B/zip=904808B；grep 确认 `DAY 4 · 推歌小吐槽` 在 built HTML 0 次、`note: 13` 在 song-data.js 0 次。articles 总数 10→9。

## 21:32 — 删除碎碎念结尾总结模块（id 18 写在最后）
- 需求：删去碎碎念板块的结尾总结模块。
- 操作：① `blog-data.js` articles 删除数组末尾 id 18「写在最后：30天推歌挑战的结尾总结」整段(`placeholder:true`)，并补回 `}` 闭合数组；② 同步在 `song-data.js` 第 160 行张国荣《我（国语版）》(DAY 30, feature「一首让你想起自己的歌」) 的 `intro` 后移除 `, note: 18`，避免简介「查看碎碎念」指向死链。
- 校验：node --check OK（两文件）；重建 HTML=1330230B/zip=904550B；grep 确认 `写在最后` 在 built HTML 0 次、`note: 18` 源码与 built HTML 均 0 次；数组末尾仅剩 id:19(DAY 29, 行198)。articles 总数 9→8。

## 21:35 — 修复主题切换按钮 emoji 图标不切换
- 现象：右上角 🌙/☀️ 深浅模式切换按钮点击后图标不变（模式实际有切换，仅图标不动）。
- 根因：按钮 HTML 为 `<button id="themeToggle">🌙</button>`，emoji 直接作按钮文本；但 index.html 内联 `initThemeToggle` 的 `setIcon()` 操作的是 `document.getElementById('themeToggleIcon')` 这个**不存在的子元素**，`if(!icon)return` 直接返回 → 图标永不更新。
- 修复：按钮内包一层 `<span id="themeToggleIcon">🌙</span>`，使 setIcon 能拿到节点。JS 逻辑本身正确无需改（初始 setIcon(cur)；点击 apply→setIcon(next)，dark→🌙 / light→☀️）。
- 校验：重建 HTML=1330264B/zip=904561B；grep 确认 `id="themeToggleIcon"` 进 built HTML 1 次。


## 21:40 — 主题切换按钮图标顺序反转
- 应用户要求把 setIcon 的图标顺序反过来：深色现显示 ☀️、浅色现显示 🌙（原为深色🌙/浅色☀️）。index.html setIcon 三元表达式改毕，重建校验通过。

## 21:42 — 更新键盘操作逻辑与玩法说明
- 玩法说明(index.html usage-list)新增：🍶漂流瓶、🌓日间/夜间模式两条；「键盘也能玩」补充弹窗内键盘操作(Tab切换、Enter跳转、Ctrl+Enter投放、Esc关闭)。
- app.js initDrift 键盘增强：漂流瓶表单「推荐曲目」Enter→跳理由框、「推荐理由」Ctrl/Cmd+Enter→requestSubmit投放；署名昵称框 Enter→确认投放。
- flipboard.js bindKeyboard 的 Esc 逻辑：当 driftModal/driftNameModal/howtoModal 任一打开时跳过「翻回全部格子」，交由各自 handler。
- 校验：node --check 三文件 OK；重建 HTML=1332265B/zip=905150B；grep 确认新文案/逻辑进产物。

## 21:48 — 方向键光标移出棋盘（打破边界圈死）
- 用户选择：到棋盘边缘时方向键把焦点移出棋盘到上方控制栏/导航。
- flipboard.js：删除 moveFocus 的边界 clamp，改为 focusInDirection / collectNavNodes 基于 getBoundingClientRect 的空间方向导航。候选节点=30格 + nav a/button + #guessBtn/#driftBtn/#howtoHint。到边缘时选方向上最近且对齐的棋盘外元素；反向再按回到棋盘。
- 保护：方向键前若 activeElement 在 .modal-overlay 内则不移动棋盘焦点（避免漂流瓶/署名/玩法弹窗抢焦点）。
- 玩法说明补充一句：到边缘会自动移到上方控制按钮。
- 校验：node --check OK；重建 HTML=1334008B/zip=905771B；grep 确认 focusInDirection/collectNavNodes 进产物、moveFocus 0 残留。

## 21:59 — 按用户三条规定重定义光标交互与说明
- (a) 不跳过翻牌棋盘：30格始终是导航节点（已满足）。
- (b) 4个可点选项纳入导航：猜一猜(#guessBtn)、漂流瓶(#driftBtn)、全部翻回(#flipBackBtn，原漏)、去听听这首(#listenBtn，原漏)；另加 hero 的"我的主页"(.netease-btn)。collectNavNodes 选择器扩展。
- (c) 玩法说明简化：去"可出/回棋盘"描述，改为"光标落在棋盘的棋格之后，按空格即可翻牌"；明确空格=确认（格子 Space 翻牌逻辑沿用 Enter/Space，按钮空格原生 click）。
- 校验：node --check OK；重建 HTML=1333825B/zip=905706B；grep 确认新选择器/简化文案进产物、旧描述0残留。

## 22:1x — 碎碎念/留言区光标 + 输入框方向键行为修复
- 需求：①碎碎念/留言区页面光标未设置；②上下左右/空格只翻页不能页面内交互；③输入框：光标落下后原生光标闪烁，方向键仍可继续移动焦点(到边界跳出输入框)。
- app.js：article-card 加 tabindex=0 role=button；gb-item 加 tabindex=0；articlesGrid 加 keydown(Enter/Space→navigateToArticle)。
- flipboard.js：bindKeyboard 去掉 homeActive 守卫→箭头在所有页面导航；新增 collectPageNavNodes()(收集激活页可聚焦节点+常驻nav)；新增 installPageFocusGuard()(MutationObserver：切到某页时把焦点放到该页首节点)；去掉 INPUT/TEXTAREA 守卫→焦点在输入框时方向键仍做空间导航(输入框保留原生光标闪烁，到边界 focusInDirection 无匹配则焦点留在输入框)。
- 校验：node --check 两文件 OK；重建 HTML=1340222B/zip=907491B。
- ⚠️ 关键运维：build_single.py 必须在沙箱外运行(dangerouslyDisableSandbox:true)，否则在沙箱中挂起、无任何输出/报错(本次挂起两次各3min+)。改用非沙箱 HTML written: 1340222 bytes 秒级完成。

## 22:1x — 碎碎念/留言区光标 + 输入框方向键行为修复
- 需求：①碎碎念/留言区页面光标未设置；②上下左右/空格只翻页不能页面内交互；③输入框：光标落下后原生光标闪烁，方向键仍可继续移动焦点(到边界跳出输入框)。
- app.js：article-card 加 tabindex=0 role=button；gb-item 加 tabindex=0；articlesGrid 加 keydown(Enter/Space→navigateToArticle)。
- flipboard.js：bindKeyboard 去掉 homeActive 守卫→箭头在所有页面导航；新增 collectPageNavNodes()(收集激活页可聚焦节点+常驻nav)；新增 installPageFocusGuard()(MutationObserver：切到某页时把焦点放到该页首节点)；去掉 INPUT/TEXTAREA 守卫→焦点在输入框时方向键仍做空间导航(输入框保留原生光标闪烁，到边界 focusInDirection 无匹配则焦点留在输入框)。
- 校验：node --check 两文件 OK；重建 HTML=1340222B/zip=907491B。
- ⚠️ 关键运维：build_single.py 必须在沙箱外运行(dangerouslyDisableSandbox:true)，否则在沙箱中挂起、无任何输出/报错(本次挂起两次各3min+)。改用非沙箱 HTML written: 1340222 bytes
ZIP written: 907491 bytes 秒级完成。
